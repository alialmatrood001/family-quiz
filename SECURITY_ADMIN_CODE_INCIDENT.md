# حادثة كشف رمز الإدارة في كود العميل

## 1. ملخص المشكلة

كانت الواجهة تحتوي على رمز إداري ثابت باسم `ADMIN_CODE`، وتتحقق منه داخل JavaScript في المتصفح عبر معامل URL. هذا لا يوفر حماية فعلية لأن كود العميل وملفات الإنتاج وطلبات المتصفح قابلة للفحص، وبالتالي يجب اعتبار أي قيمة سرية مضمنة فيها مكشوفة.

نقل الرمز إلى ملف `.env` مع اسم يبدأ بـ`VITE_` لا يحل المشكلة؛ إذ يضمّن Vite هذه القيم في حزمة الواجهة المبنية، ويمكن للمستخدم استخراجها من ملفات التطبيق.

## 2. مدى الانتشار

- الملف الحالي الذي كان متأثرًا: `src/App.jsx`.
- في Commit الحماية، كانت المواضع المتأثرة في `src/App.jsx` عند الأسطر 38 و5442 و7376 و7562 و7568 و8828 تقريبًا.
- شمل الاستخدام تعريف الرمز، مقارنته بمعامل URL، وإنشاء روابط تحتوي عليه.
- لم يظهر تخزين للرمز في `localStorage` أو `sessionStorage` أو Cookies.
- ظهر الرمز في تاريخ Git منذ أول Commit:
  `3397aaec199faea4116f322cc0b1d1a112a388d0`.
- ظهر في نحو 17 Commit وقت الفحص.
- تشير مراجع Git البعيدة المحلية إلى أن `origin/main` يحتوي على الرمز؛ لذلك يُرجح بقوة أنه وصل إلى GitHub.
- لم تظهر ملفات `.env` أو service account أو private keys أو credentials متتبعة حاليًا أو محذوفة في التاريخ الذي تم فحصه.

لا يتضمن هذا المستند قيمة الرمز.

## 3. ما تم تنفيذه

- أزيل تعريف الرمز الثابت من النسخة الحالية.
- أزيلت المقارنة بالرمز ومعامل `admin` كوسيلة حماية.
- أزيل الرمز من الروابط الداخلية.
- بقيت شاشة المتسابق هي المسار الافتراضي.
- بقيت شاشة العرض متاحة عبر `?view=display`.
- أصبحت طلبات عروض الإدارة معطلة افتراضيًا في Production وتعرض رسالة واضحة بدل لوحة الإدارة.
- يوجد تجاوز تطوير محلي باسم `LOCAL_ADMIN_DEV_BYPASS`، ويعتمد حصريًا على `import.meta.env.DEV === true`، ولا يعتمد على رمز أو كلمة مرور أو متغير `VITE_*`.

الملفات المتغيرة في هذه العملية:

- `src/App.jsx`
- `SECURITY_ADMIN_CODE_INCIDENT.md`

## 4. الإجراءات اليدوية المطلوبة من مالك المشروع

- اعتبر الرمز السابق مكشوفًا ولا تُعد استخدامه.
- غيّر أي رمز مماثل مستخدم خارج هذا المشروع.
- راجع مستودع GitHub والمشاركين وأي نسخ أو سجلات قد تكون احتفظت بالقيمة.
- لا ترسل قيمة الرمز في محادثة أو Issue أو ملف توثيق.
- نسّق عملية مستقلة إذا تقرر تنظيف تاريخ Git.

## 5. الحل النهائي المخطط

- تفعيل Firebase Authentication للمشرفين.
- منح صلاحية الإدارة عبر Admin Custom Claims أو تحقق موثوق من جهة الخادم.
- فرض الصلاحيات بواسطة Firestore Rules.
- نقل العمليات الحساسة إلى Cloud Functions مع تحقق من الهوية والصلاحية.
- إزالة أي حماية إدارية تعتمد على معاملات URL أو حالة JavaScript في المتصفح.

## 6. قرار تاريخ Git

يحتاج تاريخ Git إلى قرار تنظيف مستقل لأن الرمز موجود منذ أول Commit ووصل على الأرجح إلى الفرع البعيد. لم يُعدّل التاريخ في هذه العملية.

إعادة كتابة التاريخ تؤثر في النسخ المحلية والفروع البعيدة والمساهمين، ولذلك لا ينبغي تنفيذها إلا بعد تدوير الرمز والتنسيق مع جميع الأطراف ووضع خطة واضحة للنسخ الاحتياطية وإعادة المزامنة.
