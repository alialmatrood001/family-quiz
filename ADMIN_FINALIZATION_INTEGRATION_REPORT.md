# تقرير العملية 5: ربط الإدارة بالاعتماد الرسمي للنتائج

## 1. الهدف

ربط واجهة React بمصادقة Firebase الإدارية وبالدالة المحصنة `finalizeQuestion`، وجعل نتيجة السيرفر هي المصدر الرسمي الوحيد لإنهاء السؤال واحتساب النقاط والترتيب، من دون نشر أو اتصال ببيانات الإنتاج.

## 2. التدفق السابق

كان `src/App.jsx` يملك مسار احتساب كاملًا داخل المتصفح:

- `calculateResultsForCurrentQuestion` يقرأ الإجابات واللاعبين ويحسب النقاط والترتيب.
- `claimQuestionProcessing` يحجز المعالجة محليًا.
- `showResultsFast` و`forceProcessResults` يستدعيان الاحتساب المحلي.
- `skipQuestionCalculation` يعلن اكتمال السؤال من React بلا احتساب.
- `AutoProcessResults` يستطيع تشغيل المسار تلقائيًا.
- شاشة العرض العامة كانت تحتوي مكونات آلية وأزرارًا تستطيع الكتابة إلى Firestore.

كان هذا مسارًا منافسًا للدالة الرسمية، ويعتمد جزئيًا على حقول إجابة يرسلها العميل.

## 3. التدفق الجديد

```text
Admin Login
→ التحقق من admin === true داخل ID token
→ إظهار/إنهاء نتائج السؤال من لوحة الإدارة
→ Callable finalizeQuestion({ roomId, questionId })
→ Server Transaction
→ rooms/{roomId}/questionResults/{questionId}
→ onSnapshot في عميل React
→ تحديث الغرفة واللاعبين من السيرفر
→ شاشة النتائج
```

الاستدعاء الوحيد موجود في `src/finalize-question-client.js`. لا يرسل العميل نقاطًا أو إجابات أو حالة صحة أو جوكر أو Snapshot؛ المدخلان الوحيدان هما `roomId` و`questionId`.

## 4. المصادقة

### التطوير المحلي

1. شغّل المحاكيات باستخدام `npm run dev:emulators`.
2. في نافذة محلية أخرى عيّن مؤقتًا:
   - `FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099`
   - `FIREBASE_PROJECT_ID=family-quiz-b7960`
   - `ADMIN_EMAIL` إلى بريد الاختبار المحلي.
   - `ADMIN_PASSWORD` إلى كلمة مرور محلية غير محفوظة في Git.
3. شغّل `npm run emulators:create-admin`.
4. شغّل الواجهة باستخدام `npm run dev` وافتح `?view=control`.
5. احذف متغير كلمة المرور من بيئة الطرفية بعد إنشاء الحساب.

السكريبت يرفض العمل ما لم يكن Auth Emulator على localhost. اختبارات التكامل تولد بريدًا وكلمة مرور عشوائيين وقت التشغيل، وتزيل المستخدمين عند الانتهاء.

### التحقق من Claim

`src/admin-auth.js` يعتمد على Firebase Auth SDK، ويقرأ ID token claims. لا تعتبر الواجهة المستخدم Admin إلا عندما يكون:

```js
user && claims.admin === true
```

يمكن تحديث token بعد إضافة Claim، وتراقب الواجهة تغيّر الجلسة بواسطة `onIdTokenChanged`. لا تخزن الواجهة token أو كلمة المرور يدويًا.

### الحالات

- غير مصادق: تظهر شاشة تسجيل الدخول فقط، ولا تُركب أدوات الإدارة.
- مصادق بلا Claim: تظهر رسالة رفض وزرا تحديث الصلاحية والخروج، ولا تُركب الأدوات.
- Admin: تظهر لوحة الإدارة مع البريد وزر الخروج.
- انتهاء الجلسة أو الخروج: تختفي أدوات الإدارة فورًا ويلغى انتظار النتيجة.

### الإنتاج

بوابة المصادقة مفروضة أيضًا في Production ولا يوجد bypass. لم يُنشأ حساب إنتاجي ولم تُضف Claims إنتاجية ولم يحدث نشر؛ يلزم تجهيز حساب Admin وClaim آمنين في عملية لاحقة قبل الاستخدام الإنتاجي.

## 5. إزالة الاحتساب المحلي

أزيلت من `src/App.jsx` الدوال والمسارات التالية:

- `claimQuestionProcessing`
- `calculateResultsForCurrentQuestion`
- `showResults`
- `showResultsFast`
- `forceProcessResults`
- `skipQuestionCalculation`
- `AutoProcessResults`

كما أزيلت أزرار إعادة الاحتساب والتجاوز من واجهة العرض، وأزيلت المكونات الآلية والكتابات من صفحة `?view=display`. الصفحة العامة الآن للعرض فقط ولا تستدعي `finalizeQuestion`.

لم يعد React يكتب نقاط السؤال أو ترتيب النتائج أو `questionResults` أو `resultsDisplaySnapshot`. ما بقي:

- تعديل نقاط يدوي مستقل وواضح داخل لوحة Admin؛ ليس احتسابًا لسؤال.
- تصفير نقاط التجربة قبل بدء المسابقة الفعلية؛ جزء من فصل Practice عن المسابقة.
- بناء Preview محلي تاريخي للعرض فقط من بيانات موجودة؛ لا يكتب Firestore ولا يعلن اكتمال السؤال.
- إرسال اختيار اللاعب وتفعيل الجوكر ما زالا من العميل، وهما ضمن المخاطر المتبقية.

## 6. حالات الواجهة

يدير `useQuestionFinalization` الحالات التالية:

- `idle`: لم يطلب الاعتماد.
- `requesting`: جارٍ إرسال Callable.
- `processing`: قبل السيرفر الطلب والواجهة تنتظر المستند الرسمي.
- `completed`: وصل مستند النتيجة الرسمي.
- `error`: فشل واضح مع رسالة عربية وإعادة محاولة يدوية.

تعطل الأزرار أثناء `requesting` و`processing`. شاشة اللاعب لا تعرض نتيجة السؤال الرسمية حتى يطابق `processedQuestionId` السؤال الحالي، ثم تقرأ النقاط والصحة من Snapshot الرسمي في الغرفة بدل حقول الإجابة التي يرسلها العميل.

## 7. التكرار والتزامن

- واجهة React تحتفظ بعملية واحدة جارية للسؤال وتعيد Promise نفسها للضغط المتكرر.
- خدمة العميل تملك Single-flight مستقلًا بحسب `roomId/questionId`.
- الاستدعاء المتزامن لا يضاعف النقاط.
- `aborted`/busy يتحول إلى انتظار مستند النتيجة بدل بدء حساب محلي.
- الاستدعاء بعد اكتمال السؤال يعامل `already-finalized` كنجاح وينتظر/يقرأ المستند الموجود.
- عند تجاوز مهلة الانتظار تظهر رسالة تأخر ويمكن إعادة المحاولة بأمان.
- حماية السيرفر بالمعاملة والقفل وIdempotency بقيت دون تعديل.

## 8. Auto End

مكونات الأتمتة التي تكتب بيانات لا تُركب إلا داخل مسار Admin المصادق. انتهاء المؤقت يغلق الإجابات وينقل العرض إلى كشف الإجابة كما كان سابقًا، ولا يحتسب نقاطًا محليًا. اعتماد النتائج اللاحق يمر حصريًا عبر خدمة `finalizeQuestion`. عند إعادة تحميل Admin أثناء معالجة قائمة، يعيد مكون الاستئناف الاستدعاء الآمن ويتابع مستند النتيجة؛ قفل السيرفر يمنع التكرار.

صفحة العرض العامة وصفحة اللاعب لا تستدعيان الدالة ولا تملكان أتمتة كتابة. نتيجة ذلك أن التشغيل التلقائي الذي يحتاج كتابة يتطلب بقاء جلسة Admin مصادق عليها مفتوحة.

## 9. Practice Mode

الأسئلة التجريبية تستخدم الدالة الرسمية نفسها للاعتماد وعرض النتائج. بعد انتهاء التجربة يبقى التصفير المقصود لنقاط التجربة قبل المسابقة الفعلية. لم يتغير منطق الجوكر أو النقاط داخل Cloud Function.

## 10. Test Mode

Test Mode ما زال ينشئ لاعبين وإجابات وهمية، لكن اعتماد النتائج يمر عبر السيرفر فقط. اختبار 50 لاعبًا نجح ثلاث مرات:

- Run 1: `2400.82 ms`
- Run 2: `231.11 ms`
- Run 3: `214.57 ms`

لا يوجد احتساب React موازٍ في هذه الاختبارات.

## 11. الاختبارات

| الاختبار | النتيجة | الزمن الملحوظ | الملاحظات |
| --- | --- | ---: | --- |
| `npm run lint` | ناجح | — | بلا أخطاء أو تحذيرات ESLint |
| `npm run build` | ناجح | `542 ms` | تحذير حجم chunk أكبر من 500 kB فقط |
| `npm run test:unit` | `8/8` | `1.45 s` | منطق النقاط والتحقق والجوكر والترتيب |
| `npm run test:baseline` | `19/19` + Unit `8/8` | `16.31 s` للتكامل | يشمل Auth وedge cases والتزامن و50 لاعبًا |
| `npm run test:admin-flow` | `1/1` | `6.36 s` | عميل Firebase نفسه: no-auth، مستخدم عادي، Claim، Admin، ضغط مزدوج، reload، timeout، logout |
| `npm run test:finalization-ui` | `1/1` | `6.05 s` | إعادة نهائية لاختبار تدفق الواجهة بعد التعديلات |
| `npm run emulators:create-admin` داخل Auth Emulator | ناجح | — | حساب عشوائي مؤقت، Claim إداري، ثم إغلاق المحاكي |

تم تشغيل الاختبارات كلها على المشروع الوهمي `demo-family-quiz` داخل Emulator Suite. أغلقت `emulators:exec` جميع المحاكيات بعد كل تشغيل، ولم تُستخدم بيانات اعتماد service account.

## 12. المخاطر المتبقية

1. **عالية:** إعداد حساب Admin وCustom Claim للإنتاج لم يتم بعد؛ لم يكن ذلك مسموحًا في هذه العملية.
2. **عالية:** إرسال الإجابات وتفعيل الجوكر ووقت بدء السؤال ما زالت كتابات مباشرة من العميل وتحتاج تحصينًا بقواعد أو Functions.
3. **عالية:** قواعد التطوير المفتوحة لم تتغير، ويجب عدم نشرها كما هي.
4. **متوسطة:** أتمتة انتهاء المؤقت تعتمد على وجود تبويب Admin مصادق مفتوح؛ صفحة العرض أصبحت سلبية عمدًا.
5. **متوسطة:** تعديل النقاط اليدوي المستقل ما زال يكتب من لوحة Admin إلى Firestore، ويحتاج مسارًا مدققًا على السيرفر لاحقًا.
6. **منخفضة:** Preview التاريخي يبني تمثيلًا محليًا غير رسمي للعرض فقط من الإجابات القديمة؛ لا يكتب أي نتائج.
7. **منخفضة:** إنتاج Vite يصدر تحذيرًا لحجم JavaScript chunk، من دون فشل بناء.
8. **منخفضة:** Firebase CLI أصدر تحذيرًا بأن إصدار `firebase-functions` يحتاج مراجعة؛ لم تُحدّث أي حزمة ضمن حدود العملية.

## 13. العملية التالية

العملية التالية الموصى بها: تحصين إرسال الإجابات وتفعيل الجوكر وبدء السؤال، ونقل الكتابات الحساسة المتبقية إلى Rules محكمة أو Cloud Functions مصادق عليها، مع إضافة تدقيق لمسار تعديل النقاط اليدوي. لا تنفذ هذه التوصية ضمن العملية 5.

## مراجعة الأمان والنطاق

- لا يوجد رمز إدارة قديم أو bypass تطويري.
- Query parameter يحدد صفحة الدخول فقط ولا يمنح صلاحية.
- لا توجد كلمات مرور ثابتة أو ملفات service account أو مفاتيح خاصة متتبعة.
- `.env` و`.env.*` متجاهلة، مع السماح فقط بملف مثال إن أضيف لاحقًا.
- حزمة Production لا تحتوي `127.0.0.1` ولا منافذ Emulator المعروفة.
- `functions/finalize-question` وFirebase Rules لم تتغير.
- لم يحدث Deploy أو Push أو اتصال ببيانات الإنتاج.
