# تقرير تحصين الكتابات الحساسة — العملية 6

## 1. الهدف

نُقلت الكتابات التي تؤثر في نزاهة المسابقة من React إلى Cloud Functions: تسجيل اللاعب وربطه بالهوية، إرسال الإجابة، الجوكر، تجهيز السؤال وبدؤه وتوقيته وكشفه، تعديل النقاط، تصفير التدريب، ومسح بيانات المسابقة الحساسة. أصبحت ساعة الخادم وحالة Firestore الرسمية هما المرجع، بينما بقيت React واجهة طلب وعرض فقط.

لم تتغير معادلة النقاط أو قواعد الجوكر: الجوكر قبل السؤال ×3، وأثناء السؤال ×2، والإجابة الخاطئة مع الجوكر تخصم النقاط الأساسية.

## 2. الجرد السابق

| الكتابة المباشرة السابقة في React | التصنيف | الإجراء |
|---|---|---|
| إنشاء اللاعب مع `score` وحالة الجوكر | حساسة | نُقلت إلى `registerPlayer` |
| إنشاء مستند الإجابة مع `isCorrect` و`points` ووقت العميل | حرجة | نُقلت إلى `submitAnswer` بعقد أدنى |
| تفعيل/إلغاء/قفل الجوكر في مستند اللاعب | حرجة | نُقلت إلى `activateJoker` و`cancelJoker` و`startQuestion` |
| كتابة السؤال الحالي وأوقات البداية/النهاية وفتح الإجابات | حرجة | نُقلت إلى `prepareQuestion` و`startQuestion` و`controlQuestion` |
| الانتقال إلى الكشف وإغلاق الإجابات وتمديد الوقت وأوقات الوسائط | حرجة | نُقلت إلى `controlQuestion` |
| تعديل نقاط اللاعب وترتيبه يدويًا | حرجة | نُقلت إلى `adjustPlayerScore` |
| تصفير نقاط التدريب وحالة الجوكر التدريبي | حرجة | نُقلت إلى `resetPracticeScores` |
| حذف اللاعبين/الإجابات من أدوات التهيئة | حساسة | نُقلت إلى `resetQuizData` |
| إنشاء اللاعبين والإجابات الوهمية من Test Mode | حساسة | أزيل من React؛ البديل اختبارات Emulator |
| تعديل الاسم/الملف الشخصي | إداري غير حساس للنتيجة | بقي مباشرًا ومقيدًا في Rules |
| الرسائل والتصويت والاستفتاء وإعدادات العرض والأسئلة | غير حساس للنتيجة | بقي مباشرًا ومقيدًا بالصلاحية المناسبة |
| قفل واجهة الإجابة والحسابات البصرية المؤقتة | محلي فقط | لا يكتب في Firebase ولا يعد نتيجة رسمية |

المراجعة النهائية لـ`src` لم تجد إنشاءً أو تعديلًا مباشرًا للإجابات الرسمية، النقاط، الترتيب، حالة الجوكر، توقيت السؤال، فتح الإجابات، النتائج الرسمية، أو Audit Logs.

## 3. الدوال الجديدة

| الدالة | العقد | الهوية والصلاحية | النتيجة الأساسية |
|---|---|---|---|
| `registerPlayer` | `{roomId,name,emoji,fullName,phone}` | أي مستخدم Firebase Auth | `playerId` مرتبط بـ`authUid` |
| `submitAnswer` | `{roomId,questionId,playerId,selectedIndex}` | مالك اللاعب فقط | `receivedAtMs` وحالة استلام عامة |
| `activateJoker` | `{roomId,questionId,playerId}` | مالك اللاعب فقط | الحالة والمضاعف الذي حدده الخادم |
| `cancelJoker` | `{roomId,playerId}` | مالك اللاعب فقط | إلغاء pending قبل بدء السؤال فقط |
| `prepareQuestion` | `{roomId,questionId,questionIndex,selectedCategory?}` | Admin Claim | سؤال عام منزوع الإجابة + سر خادمي |
| `startQuestion` | `{roomId,questionId}` | Admin Claim | وقت بدء خادمي وحالة `started` |
| `controlQuestion` | `{roomId,questionId,action,seconds?}` | Admin Claim | كشف/تمديد/بدء أو إنهاء الوسائط |
| `adjustPlayerScore` | `{roomId,playerId,delta,reason}` | Admin Claim | before/after و`runId` |
| `resetPracticeScores` | `{roomId,reason}` | Admin Claim | تصفير ذري بعد اعتماد نتيجة التدريب |
| `resetQuizData` | `{roomId,mode,reason}` | Admin Claim | مسح منظم مع Audit Log |

ترفض الدوال الحقول غير المتوقعة. أهم رموز الأخطاء: `unauthenticated`، `permission-denied`، `invalid-argument`، `not-found`، `failed-precondition`، `already-exists`، و`aborted`. لا تتضمن الاستجابات الإجابة الصحيحة أو بيانات لاعب آخر أو Stack Trace.

## 4. هوية اللاعب

تستخدم صفحة اللاعب Anonymous Authentication. تستدعي `registerPlayer` بعد المصادقة، ويحفظ الخادم `request.auth.uid` في `players/{playerId}.authUid`. كل طلب إجابة أو جوكر يقرأ مستند اللاعب داخل Transaction ويطابق `authUid` مع UID الحالي.

لا تكفي قيمة `playerId` أو Local Storage أو الاسم أو الهاتف للكتابة باسم لاعب. أظهر اختبار التكامل أن UID لاعب لا يستطيع الإجابة أو تفعيل الجوكر باسم لاعب آخر.

## 5. إرسال الإجابات

المستند الرسمي ثابت:

```text
rooms/{roomId}/answers/{questionId_playerId}
```

يسمح العقد فقط بـ`selectedIndex`. ينشئ الخادم `createdAt` و`receivedAtMs` من Timestamp خادمي، ويتحقق من السؤال النشط و`acceptingAnswers` ونافذة البداية والنهاية. لا يقبل `isCorrect` أو `points` أو `answeredAt` أو المضاعف. تستخدم Transaction مع `create`، لذلك تبقى أول إجابة وحدها رسمية، ويُرفض التكرار بـ`already-exists` ولا يمكن تعديل المستند مباشرة عبر Rules.

## 6. الجوكر

- قبل السؤال: يضع الخادم pending، ثم يقفله `startQuestion` ذريًا للمضاعف ×3.
- أثناء نافذة الإجابة: يثبت الخادم المضاعف ×2، بشرط عدم وجود إجابة رسمية سابقة.
- بعد الإغلاق أو الإجابة: يُرفض التفعيل.
- التكرار لنفس السؤال Idempotent ولا يستهلك الجوكر مرتين.
- الإلغاء مسموح للـpending فقط قبل بدء السؤال؛ لا يمكن استرجاع جوكر مستخدم.
- لا يقبل العقد مضاعفًا قادمًا من العميل.

## 7. بدء السؤال

`prepareQuestion` يقرأ السؤال الإداري، ينقل الإجابة الصحيحة وحقول الاحتساب إلى:

```text
rooms/{roomId}/questionSecrets/{questionId}
```

ويضع نسخة عامة في `room.currentQuestion` بلا `correctIndex`. `startQuestion` يتطلب Admin Claim، ويكتب Timestamp خادميًا و`activeQuestionId` و`acceptingAnswers` وأوقات النافذة. تكرار نفس الطلب يعيد `already-started` بنفس الوقت، ومحاولة بدء سؤال مختلف أثناء سؤال نشط تُرفض. `controlQuestion` وحدها تمدد الوقت أو تدير الوسائط أو تغلق الإجابات وتكشف الصحيح.

## 8. تعديل النقاط

`adjustPlayerScore` يقبل delta صحيحًا بين ±10000 وسببًا من 3 إلى 200 حرف. تنفذ Transaction تعديل النقاط وإعادة ترتيب جميع اللاعبين معًا، وتمنع فقدان أحد تعديلين متزامنين.

سجل التدقيق محفوظ في:

```text
rooms/{roomId}/auditLogs/{logId}
```

ويشمل نوع العملية وAdmin UID والهدف وbefore/after وdelta والسبب وTimestamp خادمي و`runId`، من دون أسرار أو بيانات اتصال.

## 9. Rules

أصبحت الكتابات التالية ممنوعة من Firebase Web SDK:

- إنشاء/تعديل/حذف الإجابات الرسمية.
- تعديل نقاط اللاعب أو ترتيبه أو حقول الجوكر.
- بدء السؤال أو كتابة أوقاته أو فتح الإجابات أو كشف النتائج مباشرة.
- قراءة أو كتابة `questionSecrets`.
- كتابة `questionResults` أو `auditLogs`.
- قراءة بنك الأسئلة الذي يحتوي الإجابات لغير Admin.
- أي كتابة في Realtime Database؛ القراءة فقط باقية لأن Presence غير مستخدم.

يسمح Rules فقط بتحديثات الملف الشخصي المحددة، Visitor الذاتي المرتبط بـUID، الرسائل المملوكة، والتصويت/الاستفتاء غير المرتبطين بالنقاط. Admin SDK داخل Functions يتجاوز Rules وفق تصميم Firebase، بعد تحقق الدوال من الهوية والصلاحية.

## 10. التوافق مع `finalizeQuestion`

لم تتغير معادلة النقاط أو الفرز أو احتساب الجوكر. عُدلت `finalizeQuestion` فقط لقراءة `questionSecrets` داخل Transaction ودمج الحقول الموثوقة خادميًا، مع دعم مرحلة `reveal`. بقي دعم Fixtures القديمة المحدود عندما لا يوجد Secret.

تقرأ `finalizeQuestion` `answer.createdAt` الخادمي و`selectedIndex` فقط، وتتجاهل أي حقول نتيجة قديمة. بعد الاعتماد، تدمج Hooks العرض بيانات `questionResults` الرسمية مع الإجابات الخام لأغراض التقارير.

## 11. حالات السباق

- إجابتان متزامنتان للاعب والسؤال نفسيهما: مستند واحد فقط؛ الأخرى `already-exists`.
- بدء السؤال مرتين: الوقت نفسه ولا تبدأ نافذتان.
- بدء سؤال مختلف أثناء نشاط سؤال: مرفوض.
- تفعيل الجوكر المتكرر: لا يوجد استهلاك مزدوج.
- إلغاء الجوكر بعد البدء: مرفوض.
- تعديلان إداريان متزامنان: كلاهما محفوظ، والنقاط النهائية تساوي مجموعهما.
- إجابة بعد الكشف: مرفوضة.
- محاولات Web SDK المباشرة للإجابة والنقاط والتوقيت والسر: رفضتها Rules.

## 12. الأداء

الاختبار استخدم مشروع `demo-family-quiz` وAuth/Functions/Firestore/Database Emulators فقط، مع 120 ثانية لسؤال Fixture كي لا يقيس بطء تشغيل المحاكي كإجابة متأخرة.

| الجولة | اللاعبون | البدء ms | الإجابات المتزامنة ms | `finalizeQuestion` ms | التدفق الكامل ms |
|---|---:|---:|---:|---:|---:|
| 1 (Cold) | 50 | 3824.64 | 31879.18 | 2217.30 | 39950.52 |
| 2 | 50 | 192.06 | 1086.50 | 293.22 | 1684.05 |
| 3 | 50 | 193.07 | 714.46 | 248.68 | 1246.02 |
| 4 | 100 | 186.78 | 1320.35 | 290.49 | 1920.04 |

Baseline العملية 5 لـ50 لاعبًا بقي ناجحًا ثلاث مرات، وكانت أزمنة التدفق القديم 2354.09 ms (Cold)، ثم 211.25 و232.38 ms. المقارنة ليست قياس إنتاج؛ التدفق الجديد يقيس أيضًا بدء السؤال والتحقق من Auth وكل Callable إجابة.

## 13. المخاطر المتبقية

- **عالية:** لم تُنشر Functions أو Rules، ولم يُفعّل Anonymous Auth في الإنتاج ضمن هذه العملية.
- **عالية (خصوصية):** مجموعة `players` ما زالت مقروءة للعرض العام وفق البنية الحالية وتحتوي الاسم الكامل والهاتف؛ فصل بيانات الاتصال إلى مستند خاص موصى به قبل Staging.
- **متوسطة:** لا توجد استعادة حساب للاعب Anonymous عند فقدان تخزين المتصفح.
- **متوسطة:** تحديثات التصويت والاستفتاء غير المرتبطة بالنقاط مقيدة بالمصادقة والحقول، لكنها لا تزال تسمح لمستخدم مصادق بالتأثير في خرائط مشاركين آخرين.
- **متوسطة:** الحد الذري الحالي 400 لاعب في البدء والاحتساب والتصفير/التعديل.
- **منخفضة:** Firebase Emulator حذر من أن إصدار `firebase-functions` قديم؛ لم تُثبت حزم جديدة ضمن العملية.
- **منخفضة:** لا يوجد App Check، ومراقبة الأخطاء الإنتاجية لم تُجهز بعد.

لا توجد ملفات Service Account أو مفاتيح خاصة أو كلمات مرور متتبعة. مفتاح Firebase Web العام الموجود مسبقًا ليس سرًا إداريًا. ملفات Debug الخاصة بالمحاكيات متجاهلة من Git، وتوقفت المحاكيات وتحررت المنافذ بعد كل تشغيل.

## 14. العملية التالية

إنشاء بيئة Staging منفصلة، تفعيل Anonymous Auth فيها، فصل بيانات الاتصال الخاصة باللاعب عن مستند العرض العام، ثم نشر Functions وRules تدريجيًا إلى Staging فقط مع Smoke Tests ومراقبة الأخطاء. لا يُنفذ أي Deploy ضمن العملية 6.

## نتائج الفحوصات

- `npm run build`: ناجح؛ تحذير حجم Chunk فقط.
- `npm run lint`: ناجح بلا أخطاء أو تحذيرات ESLint.
- Unit: `12/12`.
- Baseline Integration: `19/19`.
- Secure Full Flow + Rules: `2/2`.
- Admin/client flow: `1/1`.
- 50 لاعبًا ثلاث مرات + 100 لاعب: `1/1` (أربع جولات داخل الاختبار).
- Race/Auth/Joker/Start/Score: ناجحة ضمن Full Flow.
- فحص رفض التشغيل خارج Emulator: Helpers تتوقف فورًا عند غياب متغيرات Emulator أو استخدام Project غير تجريبي أو وجود Service Account.
- Production bundle: لا يحتوي `127.0.0.1` أو `demo-family-quiz` أو منفذ Functions Emulator `5001`.
- الأوامر الأساسية: `npm.cmd run build`، `npm.cmd run lint`، `npm.cmd run test:unit`، ومجموعات `firebase.cmd emulators:exec --project demo-family-quiz ...`.
- لم يحدث Push أو Deploy أو اتصال ببيانات الإنتاج.
